# 07 验证记录

## 单元测试

执行命令：

```bash
npm test
```

验证结果：

```text
✔ buildBrief turns a raw product idea into an AI-coding-ready brief
✔ buildBrief rejects an empty product idea
ℹ tests 2
ℹ pass 2
ℹ fail 0
```

这证明核心生成规则当前可用。

## 本地页面

执行命令：

```bash
npm start
```

打开：

```text
http://localhost:5173
```

验证输入：

```text
产品想法：做一个给上班族用的个人效率计划工具
目标用户：上班族、产品经理、运营人员
核心功能：记录任务和日程，按优先级排序，标记完成状态
体验风格：清爽，适合长时间使用
限制条件：先做本地单页版本，不接后端
```

页面生成结果包含：

```text
产品名称：上班族个人效率计划工具
一句话想法：做一个给上班族用的个人效率计划工具
目标用户：上班族、产品经理、运营人员
核心功能：记录任务和日程，按优先级排序，标记完成状态
体验风格：清爽，适合长时间使用
限制条件：先做本地单页版本，不接后端
```

## 复制验证

点击“复制提示词”后，剪贴板内容包含：

```text
产品名称：上班族个人效率计划工具
```

浏览器控制台没有错误日志。

## 截图

运行结果截图：

```text
landing-page-tool-result.jpg
```

## 结论

这个项目不是只看源码完成，而是经过了三层验证：

- 测试验证核心规则。
- 浏览器验证页面交互。
- 剪贴板验证复制动作。
