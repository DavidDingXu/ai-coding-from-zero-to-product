# 02-spec

范围：

- 保留反馈原文、渠道、用户和时间。
- 把反馈分成 usability、trust、commercial、bug、feature-request。
- 提取 fact、judgment、assumption 和 needsEvidence。
- 生成一个可执行 spec。
- 生成小任务队列，每个任务有验证方式。
- evidence 不齐时 audit 为 `blocked`。

不做：

- 不直接让 AI 改代码。
- 不把单条反馈直接当需求。
- 不承诺没有排期的功能。
