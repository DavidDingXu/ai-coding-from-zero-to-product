# 08-review

这版做对的事：

- 部署不是直接执行云命令，而是先形成 runbook。
- 平台选择按国内访问、项目能力和后端需求判断。
- 环境变量、preview、smoke test 和 rollback 都被纳入证据。
- 默认 audit 为 blocked，避免读者误以为有命令就能上线。

后续要注意：

- 第 24 篇域名、SEO 和统计要复用 preview / production 的区分。
- 第 25 篇商业化和支付沙箱要复用环境变量和回滚边界。
- 平台命令会变化，正式发布前必须重新核对官方文档。
