# 02-spec

范围：

- 定义本地沙箱、支付宝沙箱和微信支付测试路线的选择边界。
- 创建沙箱订单，默认 `pending`。
- 验证 Webhook 签名，签名失败不能开通权益。
- 支付成功后订单变 `paid`，权益变 active。
- 重复事件只能处理一次。
- 退款后订单变 `refunded`，权益撤销。
- evidence 不齐时 audit 为 `blocked`。

不做：

- 不接真实支付通道。
- 不保存生产支付密钥。
- 不替代商户资质、税务或合规判断。
