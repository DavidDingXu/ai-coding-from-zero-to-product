# 08 review

这一轮做对的地方：

- 先用测试锁住范围，没有把官网项目扩大成完整应用。
- 页面和 CLI 共用 `landing-page.js`，文案、校验和文章证据能对齐。
- 表单明确拦截密钥类输入，避免把 API Key 填进反馈框。
- README 里写清楚当前不接真实模型、不写远程数据库。

后续要注意：

- 第 19 篇进入网页应用时，才能开始做今日计划生成。
- 如果接入真实模型，必须从 `.env` 和后端边界开始，不要把 Key 写进前端。
- 如果要保存候补数据，需要新增后端或低代码数据服务，并补隐私提示。

迁移到自己的产品时，先替换三件事：

- 产品一句话。
- 三个价值点。
- 候补表单要收集的最小字段。
