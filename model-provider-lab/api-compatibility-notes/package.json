{
  "name": "api-compatibility-notes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assess": "node src/cli.js",
    "test": "node --test",
    "verify": "npm test && npm run assess && node src/cli.js --mode compare && node src/cli.js --mode checks"
  }
}
